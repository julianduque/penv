#!/usr/bin/env node

var colors = require('colors'),
	penv = require('../lib/penv');

penv.start(function (err, file) {
  if (err) {
    return console.log(err);
  }
  console.log('success: '.green + ' customized ' + file.grey + ' created');
});